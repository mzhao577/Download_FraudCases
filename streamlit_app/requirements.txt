# The PDF pages are rendered to images with pypdfium2 rather than handed to
# st.pdf, so streamlit-pdf is deliberately not required: its viewer lays the
# page out inside its own iframe at a width this app cannot control.
streamlit>=1.54
pandas>=2.0
pypdfium2>=4.0
